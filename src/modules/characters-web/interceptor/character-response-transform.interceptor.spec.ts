import { CharacterResponseTransformInterceptor } from './character-response-transform.interceptor';

describe('CharacterResponseTransformInterceptor', () => {
  it('should be defined', () => {
    expect(new CharacterResponseTransformInterceptor()).toBeDefined();
  });
});
